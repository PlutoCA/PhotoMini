<!--pages/index/index.wxml-->
<!-- <view  wx:for="{{images}}">
{{index}}
</view>
<view wx:for="{{array}}">
  {{index}}: {{item.message}}
</view> -->
<image wx:for="{{images}}" src="{{item.fileID}}"></image>
